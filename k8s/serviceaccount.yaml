# Bound to the agent IAM role by the EKS Pod Identity association in
# terraform/envs/eks-demo. No access keys anywhere: the pod receives the same identity the
# Fargate task uses, so the application code is byte-identical on both platforms.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: fraud-lake-api
  namespace: fraud-lake
